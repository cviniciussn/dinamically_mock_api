# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mockserver
DB_USER=mockserver
DB_PASSWORD=mockserver

# Server
PORT=3001
HTTPS_ENABLED=true

# SSL (only if HTTPS_ENABLED=true)
SSL_KEY=server.key
SSL_CERT=server.crt
